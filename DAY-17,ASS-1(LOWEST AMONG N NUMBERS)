#include<stdio.h>
int main()
{
	0int a[10],i,s,n;
	printf("Enter the array size:");
	scanf("%d",&n);
	printf("Enter the array elements:");
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	s=a[0];
	for(i=0;i<n;i++)
	{
		if(a[i]<s)
		s=a[i];
	}
	printf("Lowest among the numbers is:%d",s);
}
